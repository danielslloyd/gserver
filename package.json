{
  "name": "gserver",
  "version": "1.0.0",
  "description": "Multi-Game Browser Platform with Firebase backend",
  "main": "public/js/app.js",
  "scripts": {
    "serve": "firebase serve",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only firestore:rules,storage:rules",
    "deploy:all": "firebase deploy --only hosting,firestore:rules,storage:rules",
    "emulators": "firebase emulators:start",
    "logs": "firebase functions:log"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danielslloyd/gserver.git"
  },
  "keywords": [
    "games",
    "firebase",
    "cloud-saves",
    "browser-games",
    "gaming-platform"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^12.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
